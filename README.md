

# Repo map
```
├── src
│   └── core
│       ├── adapters         # adapters each define a trait, real and fake
│       │   └── file_system  # local file system adapter
│       └── domain
└── template_files

(generated with repo-mapper-rs)
::
```
